# Smart Parts Catalog — Server (Express + Mongoose)

## Quickstart
```bash
cp .env.example .env
npm install
npm run dev
# in another shell
npm run seed
```
API runs on `http://localhost:3000` by default.
